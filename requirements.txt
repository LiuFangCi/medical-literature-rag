# Phase 1: 資料蒐集
requests>=2.31.0
python-dotenv>=1.0.0

# Phase 3a: 本地 Embedding + 向量檢索(不需要 API Key)
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0

# Phase 3b: LLM 生成答案(需要 OpenAI API Key)
openai>=1.40.0

# 之後 Phase 會陸續加入,先不用裝:
# rank-bm25           # Phase 4: BM25 / Hybrid Search
# streamlit           # Phase 6: Demo App
